
on: push

jobs:
  docker:
    name: build_docker
    runs-on: ubuntu-20.04
    steps:
      - 
        name: build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: "ytianxia6/joplin-auto-pub:latest"

  
